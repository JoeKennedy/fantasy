<button .player-action-button .btn .btn-primary .btn-xs id="#{buttonId}" data-toggle="#{dataToggle}" data-target="#{dataTarget}" character-name="#{name}">
    #{toUpper text}
    <i class="fa fa-#{icon}">
