<i .fa .fa-lg .fa-#{icon} .text-muted>
\ &nbsp;
$if usePastTense
    #{pastTense} #
$else
    #{presentTense} #
$with leagueId <- transactionLeagueId transaction
    <a href=@{LeaguePlayerR leagueId playerId}>
        #{characterName character}
    $if showTeam
        \ to
        <b>
            $maybe Entity newTeamId newTeam <- maybeNewTeam
                \
                <a href="@{LeagueTeamR leagueId newTeamId}">
                    House #{teamName newTeam}
            $nothing
                \ Waivers
